# Server port (platforms like Render set this automatically)
PORT=8080

# Session cookie secret — change this to a random string in production
COOKIE_SECRET=change-me-to-a-random-string

# Set to "production" when deploying
NODE_ENV=production
